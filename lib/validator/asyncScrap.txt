/*
var compiledSchema = validator.compileSchema(dateSchema, function (err, compiledSchema) {
  if(err){
     console.log(JSON.stringify(err,null,4));
     return null;
  }
  else{
    //var valid = validator.validate(dateObj, compiledSchema);
    console.log(JSON.stringify(compiledSchema, null, 4));
    return compiledSchema;
  }
    //console.log(JSON.stringify(compiledSchema,null,4));
});
*/


/*
validator.validate(dateObj, compiledSchema)
    .then(function(report){
      console.log(true);
        // ...
    })
    .catch(function(err){
        console.error(err.errors)
    })

*/

/*
var valid = validator.validate(dateObj, compiledSchema);
if (!valid) {
    var error = validator.getLastError();
    console.log(JSON.stringify(error,null,4));
}
*/